# awsStudy — 프로젝트 컨텍스트

> Claude Code가 세션마다 자동으로 읽는 파일. 다른 머신에서 clone해도 동일하게 적용됨.
> 매 코딩 질문 시 깔고 가야 할 기준만 담는다. 일시적인 할 일/진행 상황은 여기 넣지 않는다.
> 자세한 내용: [학습_플랜.md](학습_플랜.md), [awsStudy_스택_가이드.md](awsStudy_스택_가이드.md)

## 프로젝트 정체성

- **목적:** 모던 스택 학습 + 사이드 프로젝트 출시용 베이스
- **스택:** Spring Boot 3.4 + JPA + Lombok / Vue 3 (Composition API) + Vite / PostgreSQL + Redis / Docker / AWS
- **사용자:** Vue 익숙. Spring Boot·JPA·모던 스택은 처음 배우는 중.

## 작업 규칙 (매번 적용)

1. **표준/관용 방식만 사용.** 모든 코드는 해당 기술의 현재 표준대로. 변칙·우회·스타일 혼용 금지.
   - Spring: 생성자 주입(`@RequiredArgsConstructor`), 레이어 분리(Controller→Service→Repository), DTO 분리, `@RestControllerAdvice` 예외처리, `application-{profile}.yml` 환경 분리.
   - Vue 3: `<script setup>` + Composition API로 통일 (Options API 혼용 X).
   - 비표준이면 명시하고, 기본은 표준 한 가지로 제시.

2. **학습 스타일 = build-first.** 코드 짜면서 막히면 묻는 방식. 설명 깊이는 "코드 짜는 데 문제없다 + 거기서 조금만 더"까지만. 묻지 않으면 내부 구현(auto-config 내부, 프록시 동작 등)은 "나중에 볼 것"으로 짧게 던지고 넘긴다. 개념 설명보다 실제 기능 만들기로 빨리 전환.

3. **답변 스타일.** 큰 제목(`#`, `##`) 쓰지 말 것. 굵은 글씨 + 텍스트로.

## 핵심 원칙 (학습 플랜)

- 모던 스택 + 사이드 **출시** = 어디 가든 플러스. 출시 못 하면 효과 절반.
- 공부 = AI 답을 의심·검증할 30% 지식. **"AI 답 받으면 무조건 한 번은 왜? 묻기"** 루틴.
- 한 번에 새 스택 여러 개 동시 도입 금지. 기본부터 굴러가게 → 출시 → 다음.
- 안 넣을 것: MSA·Kubernetes·Kafka·Redis/Elasticsearch 처음부터 (1인 사이드엔 과잉, 필요할 때·회사에서). Node.js·Next.js 우선 도입(부트 깊이가 먼저).

## 결정 기록 (Decision Log)

### RAG / LLM 도입 — "2단계 기능"으로 (기본 CRUD 출시 후)
- **방향:** 붙일 가치 충분. MSA·Kafka 같은 인프라 과잉과 다름 — 사용자에게 실제 가치 주는 기능, 2026 시장 차별화 큼.
- **타이밍:** 1단계 = 기본 CRUD 완성·출시(부트 기본기). 2단계 = 출시 후 RAG 얹기. 동시에 하면 둘 다 어설퍼지고 출시 밀림.
- **방법(인프라 최소화):** Spring AI + pgvector(지금 쓰는 PostgreSQL 확장) + LLM API 하나(OpenAI/Anthropic, 임베딩+채팅). 별도 벡터 DB 제품(Pinecone 등) 안 씀.
- **전제:** ① 도메인에 쌓이는 데이터(검색/질의응답할 콘텐츠)가 있어야 함 — 기능을 위한 기능 X. ② API는 호출당 과금 + 키 관리 필요.
- **RAG 한 줄:** 내 데이터에서 관련 부분 검색 → 질문과 함께 LLM에 넘김 → LLM이 그 조각만 근거로 생성. LLM이 내 DB를 직접 보는 게 아님. 검색은 임베딩(텍스트→의미 담은 숫자)으로 의미 비슷한 것을 찾음.

## 도메인 선택 기준 (사이드)

- "내가 매일 쓸 거" 1개. 안 쓰는 도메인은 동기 6주 안에 소진.
- 양면 구조 가능하면 좋음 (관리자=나 + 사용자=가족/친구).
- 쌓이는 데이터 있는 도메인이면 나중에 RAG 자리도 생김.
- 첫 사이드는 웹+PWA. 돈·앱·거창한 기획 X, 출시가 목표.
